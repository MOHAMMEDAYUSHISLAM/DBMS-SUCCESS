# DBMS-SUCCESS
When your application communicates with a database (like SQL Server or Oracle), every command—from a simple query to a complex transaction—returns a status code. The "Success" code tells the application what happened and how to proceed.  Database APIs categorize success.
